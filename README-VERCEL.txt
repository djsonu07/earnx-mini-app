EARNX VERCEL FIX

This version uses pinned, stable package versions and a proper Vite config.
It also includes a visible loading screen and React error screen instead of a blue/blank screen.

VERCEL SETTINGS:
Framework Preset: Vite
Build Command: npm run build
Output Directory: dist
Install Command: npm install
Root Directory: ./

IMPORTANT:
- Upload/extract the CONTENTS of this ZIP into the GitHub repository root.
- package.json and index.html must be directly in the repository root.
- Do NOT put the whole ZIP inside another folder.
- Do NOT add a catch-all vercel.json rewrite.
- Redeploy after replacing the old files.
