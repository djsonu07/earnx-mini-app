TEMPORARY TEST RULES ONLY — NOT PRODUCTION SAFE.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

IMPORTANT: For production, do NOT leave this rule. Reward, balance and withdrawal approval must be protected by trusted server logic / Firebase custom claims.
